# QR AJN Firebase Setup

QR AJN currently uses Firebase Authentication, Realtime Database and Storage.

Configured project: `unna-space-prod-226ff4`.

This project is shared with another application. Never delete unrelated Firebase data or replace unrelated global rules. Merge only the QR AJN `qrajn` namespace rules and storage match blocks.

See `PRODUCTION_SETUP.md` for Authentication providers, database rules, storage rules and server Admin environment variables.
